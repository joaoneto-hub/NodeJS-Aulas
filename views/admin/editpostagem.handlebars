{{#each error}}
   <div class="alert alert-danger">
       {{texto}}

   </div>


{{else}}

{{/each}}



<h4>Editar Postagem:</h4>
<br>
<div class="card">
<div class="card-body">
    <form action="/admin/postagem/edit" method="POST">
    <input type="hidden" value="{{Postagem._id}}" name="id">
        <label for="titulo">
        Titulo:
        </label>
            <input type="text" name="titulo" class="form-control" value="{{Postagem.titulo}}">
        <label class="mt-3" for="slug">
            Slug:
        </label>
            <input type="text" name="slug" class="form-control" value="{{Postagem.slug}}">
            <label class="mt-3" for="descricao">
            Descrição:
        </label>
            <input type="text" name="descricao" class="form-control" value="{{Postagem.descricao}}">
        <label class="mt-3" for="conteudo">
            Conteudo:
        </label>
        <textarea name="conteudo" class="form-control ">{{Postagem.conteudo}}</textarea>
        <label for="Categoria">
            <select name="categoria" class="custom-select mt-3">
                {{#each Categoria}}
                    <option value="{{_id}}">
                        {{nome}}
                    </option>
                {{else}}
                    <option value="0">
                        Nenhuma Categoria registrada
                    </option>
                {{/each}}
            </select>
        </label>
        <br>
        <button class="btn btn-success">
            Editar Postagem
        </button>
    </form>
</div>
</div>